# Releases

One row per production deploy of **frybirdiq.tech**, newest first. Append-only: never edit a past row. If a row turns out wrong, add a correction row.

- **Who deploys:** only FRYBIRD Chief (the hive orchestrator), and only after the owner says yes to that specific deploy.
- **Commit:** the full SHA that was built. Production's `/var/www/frybird/.next/BUILD_ID` must equal the local `.next/BUILD_ID` of that build.
- **Smoke:** signed-out HTTP checks on the live site (no forms, no orders, no payments).
- **Signed-in check:** a staff session walks the changed screens. Never place, pay, cancel or refund an order in production to do it.
- **Migration head:** the last migration applied in production (`drizzle.__drizzle_migrations` count vs `supabase/migrations/meta/_journal.json`).

| # | Deployed (UTC) | Commit | Branch | BUILD_ID | Migration head | Who | Smoke | Signed-in check | Evidence / approval |
|---|---|---|---|---|---|---|---|---|---|
| 0 | 2026-09-16 21:15 | `86de13321133b6108d2aa07a9450f4212fa77c12` | `kit-radix-nova` | `iF6V3Sbwg6RdvAkAnSfAn` | `0032_backfill_rls_gap` (33 of 33 applied) | Not recorded. Built and shipped from the main checkout; no hive agent deployed it | Pass, run 2026-09-16 23:56 UTC during reconstruction: `/` 200, `/menu` 200, `/sign-in` 200, `/app/orders` and `/app/finance` 307 → `/sign-in`. `/app` 404 (there is no index page at that route) | Not run. Needs a staff sign-in | Baseline reconstructed 2026-09-16 23:56 UTC, read-only: remote BUILD_ID equals the local build's; SHA-256 of every server `.js` in `.next/server/{app,chunks}` matches between local and production; a string added in `86de133` is present in the production bundle; HEAD was `86de133` from 21:04 UTC until after the 21:15 UTC build; service started 21:15:22 UTC. Deploys before this one are narrated in `FRYBIRD-IQ-PROGRESS.md` |
| 1 | 2026-09-17 10:55 | `5d7ed22` | `kit-radix-nova` (release/rc-3) | `xzUttXpC0koH-qJgLA7wL` | `0033_rls_write_lockdown` (34 of 34 applied) | god (FRYBIRD Chief), owner-approved | Pass 10:56 UTC (`/`, `/menu`, `/sign-in` 200; `/app/orders`, `/app/finance` 307); deploy.sh gates 921 unit tests; service active, 0 errors in journal | Not run (needs owner device: signed-in KDS realtime) | Owner approved rel-2 2026-09-17 10:46 UTC. Pre-check: 33/33 applied, backup 12 h. Post-check: anon/authenticated INSERT/UPDATE/DELETE grants on public = 0 (read-only query). Includes rc-1..rc-3: P0 role self-promotion fix (0033), order/payment state fixes, rider cash close, IST P&L, refund/double-capture counting, dormant IQ foundations (no 0034+). |
| 2 | 2026-09-17 12:30:57 (service start) | `6ec290c9ce1f8997e3658f76ade92ae9df69b211` | `kit-radix-nova` (release/rc-11) | `xsgdt2f8dY9xNCHCOhI1p` | `0036_iq_facts` (37 of 37 applied) | Not recorded. Owner approved rel-3 (deploy 6ec290c); the deploy itself was never logged | Not recorded at the time | Not run | Recorded late, 2026-09-19, card rel-5. Found by DEVOPS read-only VPS check 2026-09-19: `DEPLOY_COMMIT` in `/etc/frybird/env` = 6ec290c, BUILD_ID above, DEPLOY.md §10 steps 1-8 in place (nginx /api/jobs block, JOB_SECRET, jobs.header, frybird-job units, heartbeat timer). Migration head 0036 verified read-only by DATABASE (all 37 rows hash-match the repo). Scope: rc-11 = migrations 0034-0036 + job runner. |
| 3 | 2026-09-19 18:12:50 | `49f858a5fe79b3b391aa85633ada1485a0b2203a` | `release/rc-13-native-retry` (fast-forwarded into `kit-radix-nova`) | `x5cu1EHxOpfe8vYlRFl8C` | `0038_refunds_status_idempotency` (39 of 39 applied) | god (FRYBIRD Chief), owner-approved | Pass 18:13 UTC: `/` 200 and `/menu` 200 both show the closed notice ("closed right now. We open tomorrow at 11:30 AM"); `/api/health` 200 `{"status":"ok"}`; `/checkout` 307 -> `/cart` (empty cart, by design) which shows the closed notice. deploy.sh gates: 2155/2155 unit tests. Service active, 0 restarts, 0 journal errors since restart | Not run. Closed-shop notice at 375/1280 px and the checkout ASAP-disabled state need a browser with items in the cart (card p0-3e); no orders placed | Owner approved 2026-09-19 while the shop was closed (23:41 IST, hours 11:30-23:00). Backup taken immediately before migrating: `/var/backups/frybird/frybird-20260919-1811.dump`, 440,664 bytes, `pg_restore --list` exit 0 (699 TOC entries, 72 table-data). Migrations 0037 + 0038 applied by `drizzle-kit migrate` (one transaction). Contents: 8 P0 launch-blocker branches, phone/order-page fixes 505b3a7 6bf4521 7304bde c209ce3, placeOrder retry fix 5c3426f 49f858a, refund release (0038) and IQ-2 insight columns (0037) from 6ad2606. QA GO; RELIABILITY, API-INTEGRATIONS, CUSTOMER-WEB agree. Rollback is code AND database together: old code writes iq_insights without as_of, which 0037 makes NOT NULL. Alerting units (DEPLOY.md 11) shipped in the repo, NOT installed on the server. |
| 4 | 2026-09-19 19:26:37 | `383e693f3a6a349d5f5ae132ed9e6c8259f8206b` | `release/rc-14-ui` (fast-forwarded into `kit-radix-nova`) | `QfcS3cFFTWYktErhEU-8Q` | `0038_refunds_status_idempotency` (unchanged, no migration) | Claude Code session (single builder), owner-approved | Pass 19:27 UTC, signed out, GET only: `/` 200, `/menu` 200, `/sign-in` 200, `/cart` 200, `/robots.txt` 200, `/sitemap.xml` 200 (frybirdiq.tech URLs), `/opengraph-image` 200, `/api/health` 200, `/order/not-a-uuid` 404, `/order/<unknown uuid>` 404, `/no-such-page` 404, `/app/orders` 307, `/app/finance` 307. Closed notice shows on `/` and `/menu` (shop closed, 00:56 IST). Home has canonical, og:*, twitter:card and Restaurant JSON-LD with GeoCoordinates. deploy.sh gates: 2189/2189 unit tests, lint 0 errors. Service active, 0 restarts, 0 journal lines at priority err, nginx 0 5xx. NOT zero application errors: from 19:26:45, a browser tab loaded before the deploy keeps calling a Server Action id the new build does not have; `Failed to find Server Action` about every 30 s (5 in the first 2 minutes) plus 2 expected `NotSignedIn` from the signed-out /app smoke. Clears when that tab is reloaded | Not run. Nobody has viewed the loyalty lines, the branded 404 or the share card rendered; no orders placed | Owner approved 2026-09-19 (deploy while closed, UI only, no migration). Backup taken immediately before: `/var/backups/frybird/frybird-20260919-1925.dump`, 445,528 bytes, `pg_restore --list` clean, 72 table-data. Contents: seo-1 (1e0e817: sitemap, robots, canonicals, share tags, JSON-LD location, 45 s public menu cache, image sizes, media cache headers, branded 404, bad `/order/<id>` -> 404) and loy-web (b407ebb: loyalty copy on home, menu, account, order, cart, checkout). Left out: the 15 item descriptions (production data, parked). QA-release on the merged branch: typecheck 0 errors, lint 0 errors, unit 2189/2189, integration 437/437 (after applying 0037/0038 to the LOCAL test database only), build passes. Display-only diff, no migration. Rollback: code-only and safe, redeploy `49f858a` (BUILD_ID `x5cu1EHxOpfe8vYlRFl8C`, worktree `~/FRYBIRD-IQ/worktrees/deploy-49f858a`). nginx `robots.txt` Sitemap line NOT yet applied (separate step). |
| 5 | 2026-09-19 19:58 | nginx only, no code deploy (repo `deploy/nginx-frybird.conf` already carries the same line from `383e693`) | `kit-radix-nova` | unchanged `QfcS3cFFTWYktErhEU-8Q` | unchanged | Claude Code session, owner-approved | Pass 19:59 UTC: `/` 200, `/menu` 200, `/robots.txt` 200 (now includes `Sitemap: https://frybirdiq.tech/sitemap.xml`), `/sitemap.xml` 200. nginx `-t` successful, reloaded (not restarted), 0 nginx errors, 0 app restarts, 0 5xx | Not applicable | Owner approved 2026-09-19 after the deploy report. Change: one line, the `return 200` body of `location = /robots.txt` in `/etc/nginx/sites-available/frybird`. Backup taken first: `/etc/nginx/sites-available/frybird.bak-20260919-195812` (identical to the live file before the edit). Note: a first attempt made no change (edit script assertion failed on a quoting error) and was followed by a no-op `nginx -t` + reload of the unchanged file. Rollback: `cp -p /etc/nginx/sites-available/frybird.bak-20260919-195812 /etc/nginx/sites-available/frybird && nginx -t && systemctl reload nginx`. |
| 6 | 2026-09-19 20:46:09 (service start; 2026-09-20 02:16 IST, shop closed) | `9a826eeb90a94d02031b72d115e1ebbee8f29b22` | `release/rc-15-close-shop` (fast-forwarded into `kit-radix-nova`) | `dNGopFZJ3CH4nlRg2eCI-` | `0039_ordering_pause` (40 of 40 applied; head `created_at` 1790400000000) | Claude Code session (single builder), owner-approved (migration 0039 and order-placement gate) | Pass, signed out, GET only: `/` 200 (closed banner `data-ordering-banner="closed"`), `/menu` 200, `/sign-in` 200, `/cart` 200, `/robots.txt` 200, `/sitemap.xml` 200, `/api/health` 200, `/order/not-a-uuid` 404, `/app/orders` 307. Server BUILD_ID equals the local build. deploy.sh gates: 2302/2302 unit. Service active, 0 restarts, 0 journal lines at priority err, 0 nginx 5xx since restart. One `POST /app/admin/restaurant` 404 at 20:46:45 UTC: an Admin tab loaded before the deploy calling an old Server Action id (same stale-tab pattern as row 4); clears on reload | Not run by me. Rule 2 (pause -> site shows paused and refuses -> resume -> site takes orders, no order placed) is done by the owner from Admin -> Restaurant on their phone; my read-only checks and the result go in a following row | Owner approved 2026-09-20. Order: fresh dump, then 0039 applied with `pnpm db:migrate` from the Mac (verified on the server: 40 rows, 4 columns, CHECK present, no org paused), then deploy. Backup immediately before: `/var/backups/frybird/frybird-20260919-2042.dump`, 445,645 bytes, `pg_restore --list` exit 0 (702 TOC entries, 72 table-data). Contents: ops-1 Close Shop (S2 0039, S3 gate + audit, S4a POS switch, S4b Admin panel, S5 banner), the three RELIABILITY refusals fixed each with fail-first proof, one-tap reasons, menu cache cleared on every switch. QA-release on the exact commit: typecheck 0 errors, lint 0 errors, unit 2302/2302, integration 478/478 (local DB with 0039), rsc-boundaries pass, build pass. Reviews: SECURITY AGREE (no blocker), RELIABILITY AGREE (medium: pause pressed between the gate and the insert can still let one order in; card queued). 0039 undo script drilled on a throwaway database (apply, refused while paused, undo, re-apply). Rollback: code-only, redeploy `383e693` (BUILD_ID `QfcS3cFFTWYktErhEU-8Q`), 0039 stays; RESUME ORDERING FIRST (runbook in docs/MIGRATION-RECOVERY.md §4f); checked: the old build's integration suite (45 files / 437 tests) passes against a database with 0039. |
| 7 | 2026-09-19 21:28:03 (service start; 2026-09-20 02:58 IST, shop closed) | `4e6c6fef39baf97132007d4322a6d108c0837a96` | `release/rc-16-staff-ui` (fast-forwarded into `kit-radix-nova`) | `6b9c1b3950382ca5` | `0039_ordering_pause` (unchanged, no migration) | Claude Code session (single builder), standing approval (UI only, shop closed) | Pass, signed out, GET only: `/` 200, `/menu` 200, `/sign-in` 200, `/cart` 200, `/robots.txt` 200, `/sitemap.xml` 200, `/api/health` 200, `/api/version` 200 (`{"build":"6b9c1b3950382ca5"}`, equals the server's `.next/BUILD_ID` and the local build), `/order/not-a-uuid` 404, `/app/orders` `/app/pos` `/app/admin/restaurant` 307. deploy.sh gates: 2354/2354 unit. Service active, 0 restarts, 0 journal lines at priority err, 0 nginx 5xx since restart, 0 `Failed to find Server Action` | Not run by me (signed out). Nobody has viewed the pill, the reload bar or the two-step resume rendered | Deployed under the standing UI-only approval. Backup immediately before: `/var/backups/frybird/frybird-20260919-2127.dump`, 450,567 bytes, `pg_restore --list` exit 0 (702 TOC entries, 72 table-data). Contents: (1) version check: opaque build id shared by server and browser bundle, public `/api/version`, `VersionCheck` in the staff chrome (POS, KDS, orders board, Admin) shows "New version — tap to reload" and reloads by itself only after 60 s idle with no dialog, no order in progress, no filled-in field, online, 5 min cooldown; (2) shop status pill in both staff top bars (Open / Closed / Orders OFF, dot + one word at phone width, popover on desktop, bottom sheet on phone, read-only without `orders.update`), sharing the POS switch's state and dialog through `useShopSwitch`; two-step "Switch orders back on" in Admin and a confirm that is inert for 700 ms in both places; "Cancel"; "Orders not finished". Server side unchanged: no change to shop-status-actions, repositories, permissions, placeOrder or migrations (checked from the diff). QA-release on this exact commit: typecheck 0 errors, lint 0 errors, unit 2354/2354, integration 478/478, rsc-boundaries pass, build pass, contrast scripts pass. Independent review: AGREE on both cards, no blocker; fixes applied before deploy (build id covers uncommitted edits, any filled-in field blocks auto-reload, resume re-arm on a changed pause, signature covers who and why). Rollback: code-only, redeploy `9a826ee` (BUILD_ID `dNGopFZJ3CH4nlRg2eCI-`, worktree `~/Downloads/rc-15-close-shop`); no database step. |
| 8 | 2026-09-19 21:50:59 (service start; 2026-09-20 03:21 IST, shop closed) | `207aacd6ec52c0a6c7be3d5ad1c2ce7af1d09660` | `release/rc-17-recheck` (fast-forwarded into `kit-radix-nova`; deployed from the detached QA'd commit, docs commit on top) | `efd32b5a1f11057e` | `0039_ordering_pause` (unchanged, no migration) | Claude Code session (single builder), owner-approved (order-placement change) | Pass, signed out, GET only: `/` 200, `/menu` 200, `/sign-in` 200, `/cart` 200, `/robots.txt` 200, `/sitemap.xml` 200, `/api/health` 200, `/api/version` 200 (`{"build":"efd32b5a1f11057e"}`, equals the server's `.next/BUILD_ID` and the local build), `/order/not-a-uuid` 404, `/app/orders` `/app/pos` `/app/admin/restaurant` 307. deploy.sh gates: 2368/2368 unit. Service active, 0 restarts, 0 journal lines at priority err, 0 nginx 5xx since restart, 0 `Failed to find Server Action`. No order placed, no pause/resume | Not run by me (signed out, no order placed by rule). The refusal at write time has not been seen on the live site | Owner approved 2026-09-20 (deploy while closed, order placement, no migration). Backup immediately before: `/var/backups/frybird/frybird-20260919-2150.dump`, 450,567 bytes, `pg_restore --list` exit 0 (702 TOC entries, 72 table-data). Contents: `placeOrder` re-decides the ordering gate (pause, ASAP hours, scheduled slot) at the moment of writing: a fresh early re-read before any customer write (thrown, so the idempotency claim is released and a refusal is never replayed), and inside `persistOrder` the order-row insert runs in a transaction that takes `FOR SHARE` on the org row and re-decides the gate (`pauseOrdering` takes `FOR UPDATE` on the same row); the promo slot is claimed in that same transaction after the gate passes (a refusal or lost race rolls it back); a fresh gate read immediately before a Razorpay intent is created; the counter stays ungated; customer-page status read times out after 2.5 s (fails open, server still refuses). QA-release on the deployed commit: typecheck 0 errors, lint 0 errors, unit 2368/2368, integration 492/492 (new file 14/14, stable x3), rsc-boundaries pass, build pass. Reviews: payments no veto (AGREE), reliability AGREE (nothing above low), security AGREE (previous commit; no auth/scoping change in the delta). Rollback: code-only, redeploy `4e6c6fe` (BUILD_ID `6b9c1b3950382ca5`); no database step; safe at any time (checked: no migration; the old build ignores nothing new). Known limits, accepted by the owner: a pause landing in the ~1 s Razorpay round trip after an intent is created leaves one unpaid intent and no order (cannot be charged; Razorpay is off in production today); a refused attempt can leave the guest's customer record (holds no money; the customer gave those details in order to order). |
| 9 | 2026-09-19 22:41:44 (service start; 2026-09-20 04:11 IST, shop closed) | `eddba1888e1388a0d96aac3bc9dd94e2b0c0b43d` | `release/rc-18-day-off` (fast-forwarded into `kit-radix-nova`) | `f094f7a2953c1cc7` | `0040_day_off` (41 of 41 applied; head `created_at` 1790500000000) | Claude Code session (single builder), owner-approved (migration 0040 and order-placement gate) | Pass, signed out, GET only: `/` 200, `/menu` 200, `/sign-in` 200, `/cart` 200, `/robots.txt` 200, `/sitemap.xml` 200, `/api/health` 200, `/api/version` 200 (`{"build":"f094f7a2953c1cc7"}`, equals the server's `.next/BUILD_ID`), `/order/not-a-uuid` 404, `/app/orders` `/app/pos` `/app/admin/restaurant` 307. deploy.sh gates: 2456/2456 unit. Service active, 0 restarts, 0 journal lines at priority err, 0 nginx 5xx since restart, 0 `Failed to find Server Action`. After marking Tuesday: live home shows "Open daily except Tuesday" and JSON-LD `dayOfWeek` without Tuesday. No order placed, no pause/resume | Not run by me (signed out, no order placed by rule). Nobody has seen the closed-today banner, the Tuesday picker line, the pill "Closed today" or the Days closed panel rendered; first Tuesday (2026-09-22) is the observation | Owner approved 2026-09-20 (deploy while closed, migration 0040, order placement). Backup immediately before: `/var/backups/frybird/frybird-20260919-2240.dump`, 450,809 bytes, `pg_restore --list` exit 0 (702 TOC entries, 72 table-data). Order: dump, `pnpm db:migrate` from the Mac (verified read-only on the server: head 1790500000000, column present, `closed_dates` empty with RLS forced, no weekly day set), deploy, smoke, then the owner's option A: ONE audited statement set `weekly_closed_days = {2}` (Tuesday) for org `frybird` (1 row updated, 1 `weekly_closed_days_changed` audit row, actor null, note names the owner's instruction). Read-only check with the app's own logic on the live values: Mon 23:30 -> next opening Wed 11:30; Tue any time -> dayOff, next opening Wed 11:30. Contents: weekly closed days, planned closed dates with public note, "Closed for the rest of today" and "Closed until a date I pick" on the switch, write-time refusal of ASAP and pre-orders on a closed day, pre-order impact list (nothing auto-cancelled), pill "Closed today · opens Wed 11:30 AM". Reviews: security and red team no blocker/high; QA 7/7 (unit 2456, integration 521 x3). Rollback: code-only to `207aacd` (BUILD_ID `efd32b5a1f11057e`) is safe for the database, but the old code takes orders on Tuesday: switch orders off first (until switched back on), then redeploy; leave 0040; never run its down script before clearing the closures. Runbook: docs/MIGRATION-RECOVERY.md 4g |
| 10 | 2026-09-19 23:06:00 (service start; 2026-09-20 04:36 IST, shop closed) | `e4d791bd0369661c740d34ccb3ce369c98bafc6b` | `release/rc-19-iq-readiness` (fast-forwarded into `kit-radix-nova`) | `05c95a9e11be8ef4` | `0040_day_off` (unchanged, no migration) | Claude Code session (single builder), standing approval (UI only, read-only queries, shop closed) | Pass, signed out, GET only: `/` 200, `/menu` 200, `/sign-in` 200, `/cart` 200, `/robots.txt` 200, `/sitemap.xml` 200, `/api/health` 200, `/api/version` 200 (`{"build":"05c95a9e11be8ef4"}`, equals the server's `.next/BUILD_ID`), `/order/not-a-uuid` 404, `/app/orders` `/app/pos` `/app/admin/restaurant` 307. deploy.sh gates: 2483/2483 unit. Service active, 0 restarts, 0 journal lines at priority err, 0 nginx 5xx since restart, 0 `Failed to find Server Action`. The five readiness queries were also run read-only against production (counts only): they execute without error | Not run by me (the panel is behind sign-in; nobody has viewed it rendered). No order placed | Deployed under the standing UI-only approval. Backup immediately before: `/var/backups/frybird/frybird-20260919-2305.dump`, 454,183 bytes, `pg_restore --list` exit 0 (709 TOC entries, 73 table-data). Contents: IQ readiness panel on the IQ overview and the AI brief page (five scores with trend, overall percent, one action, "Limited data" badges with visible reasons on Payment methods, Top products, Attention, Channels, Net profit). Read-only; no migration; no change to money, orders, auth or permissions. Reviews: owner-guide review (3 must-fix, 8 should-fix, all fixed), QA 7/7 (unit 2483, integration 531 x3). Rollback: code-only, redeploy `eddba18` (BUILD_ID `f094f7a2953c1cc7`); nothing to undo in the database. What production will show first (counts, not customer data): orders closed the same day 22 of 67; cash recorded 64 of 64; best sellers with a recipe 1 of 20; stock: 0 of 6 ingredients with a recorded count, never counted; customers on orders 9 of 64 |
| 11 | 2026-09-20 17:39:48 UTC (service start; 23:09 IST, shop closed; pre-deploy quiet-shop check not clear at 21:25 IST, one accepted takeaway order 1264 untouched since 21:18, no website order after 20:56 IST, so deployed after the 23:00 close) | `43e240a8fd974f875b9d468962d3e7eea5775e61` | `release/rc-20-iq2` (fast-forwarded into `kit-radix-nova`) | `bfbfbc5555f9f10d` | `0040_day_off` (unchanged, no migration) | Claude Code session (single builder), owner go/no-go yes (IQ-2, timers, alerting unit files, one history fill) | Pass, signed out, GET only: `/` 200, `/menu` 200, `/sign-in` 200, `/api/health` 200, `/app/iq/brief` 307 to `/sign-in`. Service active, 0 restarts, 0 5xx in nginx, `systemctl --failed` empty. The only warning-level journal line is the old process's exit 143 at restart (same as every deploy). Nothing placed | Not run by me: the brief page rendered (behind sign-in) and the alert channel (no `ALERT_URL` exists) | Backup immediately before: `/var/backups/frybird/frybird-20260920-1737.dump`, 462,827 bytes. Contents: Daily brief page `/app/iq/brief`; reconciliation counts only SUCCEEDED refunds; detections on the alerts page. Server: four timers installed and enabled (facts nightly 20:30 UTC, reconcile 21:00, detect 21:15, brief 02:00), the alert unit files and `/usr/local/bin/frybird-notify` installed WITHOUT `alert.env`, so alerts are unconfigured and a failed job shows only in the journal and `systemctl --failed`. First manual runs, in order: iq-facts-nightly, iq-reconcile-nightly, iq-detect-daily, iq-brief-daily all SUCCEEDED; one iq-facts-backfill SUCCEEDED. Parity: iq_daily_facts revenue_net equals the live paid-order net revenue the P&L page uses on all 10 trading days 2026-09-10 to 09-19, to the paisa (SQL comparison, counts and paise only; the pages themselves not viewed). Rollback: code-only, redeploy `e4d791b` (BUILD_ID `05c95a9e11be8ef4`); remove the four timers per DEPLOY 9.6 (units and `iq_*` rows are history, no migration to undo). Previous unit files saved in `/root/iq2-units-prev` on the server |
| 12 | 2026-09-20 20:15:19 UTC (service start; 2026-09-21 01:45 IST, shop closed; quiet-shop check clear at 01:44 IST: 0 orders in progress, 0 online in 60 min, newest order 266 min old; 2h35 of clean journal since Release 7) | `317ec9f` | `release/rc-21-pay-ready` (fast-forwarded into `kit-radix-nova`) | `28643f6a942b3b75` | `0040_day_off` (unchanged, no migration) | Claude Code session (single builder), owner go/no-go yes (Razorpay readiness, no keys) | Pass, signed out, GET only: `/` 200, `/menu` 200, `/sign-in` 200, `/cart` 200, `/robots.txt` 200, `/sitemap.xml` 200, `/api/health` 200, `/api/version` 200 (equals `.next/BUILD_ID`), `/order/not-a-uuid` 404, `/app/orders` `/app/pos` `/app/iq/brief` 307. Service active, 0 restarts, 0 journal lines at err, 0 nginx 5xx, 0 `Failed to find Server Action`, `systemctl --failed` empty | Not run by me: any payment (no keys exist; I never place, pay or refund in production), the staff order board rendered | Backup immediately before: `/var/backups/frybird/frybird-20260920-2014.dump`, 520,306 bytes. Contents: unpaid online orders never reach the kitchen or Accept; retryable gateway errors (401/403/408) no longer become a final decline, the customer sees "checking with the bank" with no second payment; order page fixes (no order link from a phone number, one generic confirm answer, org-scoped `getOrder`); tests for checkout, online-payment confirm and webhook; permission-gate test that fails if any gate is removed. Reviews: payments no veto, security no blocker, architect coherent; the post-review commit `5d4ca97` was re-reviewed by payments (no veto) and security (no high/medium) before this deploy; QA 7/7 on `317ec9f` (unit 2824, integration 578 x3). Payment/order/auth paths identical between `5d4ca97` and `317ec9f`. Rollback: code-only, redeploy `754fc59` (BUILD_ID `bfbfbc5555f9f10d`); no migration |
| 13 | 2026-09-20 20:47:44 UTC (service start; 2026-09-21 02:17 IST, shop closed; quiet-shop check clear: 0 orders in progress, 0 online in 60 min, newest order 298 min old; 32 clean minutes since Release 8) | `6b09194` (deployed detached; docs and a helper script on top on the branch) | `release/rc-22-cash-sessions` (fast-forwarded into `kit-radix-nova`) | `c98ace25fed78695` | `0041_cash_sessions` (42 of 42 applied; head `created_at` 1790600000000; applied from the Mac, fail-fast, before the deploy) | Claude Code session (single builder), owner go/no-go yes (the till) after three owner-set checks | Pass, signed out, GET only: `/` 200, `/menu` 200, `/sign-in` 200, `/cart` 200, `/robots.txt` 200, `/sitemap.xml` 200, `/api/health` 200, `/api/version` 200 (equals `.next/BUILD_ID`), `/order/not-a-uuid` 404, `/app/orders` `/app/pos` `/app/finance` `/app/iq/brief` 307. Service active, 0 restarts, 0 journal lines at err, 0 nginx 5xx, 0 `Failed to find Server Action`, `systemctl --failed` empty. Migration verified read-only on production: 2 tables, 4 payments columns, `payments_cash_holder_check` present, 108 existing payments unchanged (0 rider-held, 0 in a session), 0 sessions | Not run by me: the till panel rendered (behind sign-in); no till opened, no cash recorded, nothing placed in production | Backup immediately before: `/var/backups/frybird/frybird-20260920-2046.dump`, 522,786 bytes. Contents: cash sessions (open with a float, blind close, variance against the closer), rider door cash held by the rider until a handover, daily reconciliation view on Finance; cash refunds ordered against the till close. Checks before the yes: REAL migration 0041 applied, undone (refused with a session present, allowed with none) and re-applied on a throwaway database built from the real migrations 0000-0040 (41 files, 73 tables); payments re-review of the `payments.ts` merge on `6b09194` (11 tool uses, no veto); QA 7/7 on `6b09194` (unit 2863, integration 600 x3, permission gates 128). Rollback: code-only is safe, redeploy `f36d57f` (BUILD_ID `28643f6a942b3b75`); the old code ignores the new tables and columns and cash taken meanwhile shows as unassigned; the undo script refuses once any session exists |
| 14 | 2026-09-20 21:20:49 UTC (service start; 2026-09-21 02:50 IST; quiet-shop check clear: 0 orders in progress, 0 online in 60 min, newest order 331 min old; 33 clean minutes since Release 9) | `818f514` (deployed detached; docs on top on the branch) | `release/rc-23-p0-7` (fast-forwarded into `kit-radix-nova`) | `834bda467a53e793` | `0042_rls_role_reads` (43 of 43 applied; head `created_at` 1790700000000; applied from the Mac, fail-fast, before the deploy) | Claude Code session (single builder), owner's 48-hour sprint rule (auth card: security AND red-team agree, owner membership unchanged, sign-in works, immediate rollback on a failed smoke check) | Pass, signed out, GET only: `/` 200, `/menu` 200, `/sign-in` 200, `/cart` 200, `/robots.txt` 200, `/sitemap.xml` 200, `/api/health` 200, `/api/version` 200 (equals `.next/BUILD_ID`), `/order/not-a-uuid` 404, `/app/orders` `/app/pos` `/app/staff` `/app/finance` 307. Service active, 0 restarts, 0 journal lines at err, 0 nginx 5xx, 0 `Failed to find Server Action`, `systemctl --failed` empty. Owner checks, read-only: memberships fingerprint `aa1a9d898d44` identical before the migration and after the deploy, both OWNER rows still active, GoTrue health via the public URL 200. Role simulation on production (counts only): BEFORE, every role read 108 payments, 14 customers, 7 memberships; AFTER, OWNER unchanged (108/14/7), MANAGER 108/14/1, CASHIER payments 0 customers 14 memberships 1, RIDER payments 0 customers 0 memberships 1, products 49 and order_events 634 for all | Not run by me: signing in as anyone (I never sign in or create accounts), the staff invite screen, the kitchen display's live updates | Backup immediately before: `/var/backups/frybird/frybird-20260920-2119.dump`, 533,436 bytes. Contents: `inviteStaff` applies the role ceiling before contacting Supabase Auth, so an ADMIN can no longer invite an OWNER (tests fail on the old code); migration 0042: 40 additive RESTRICTIVE select policies limiting what a staff login using the anon key can read by role (customers, payments, refunds, the books, orders, stock, recipes, integrations, memberships, ...) plus a column-level grant that makes `pos_pin_hash` unreadable to logins. Reviews: security (no blocker; delta re-review pass), red-team (agree, 13 attack routes tried, none worked; delta agree), QA 7/7 on `818f514` (unit 2868, integration 615/615 on runs 2 and 3; run 1 had one failure in the till reconciliation test that passed 22/22 alone twice, see HANDOVER 28). Undo drilled on a real schema built from migrations 0000-0041: policy set and memberships grant restored exactly. Rollback: code-only is safe and irrelevant (the code never depends on the policies); undo script `supabase/rollback/0042_rls_role_reads.down.sql` drops the 40 policies and restores the grant, no data touched |
| 15 | 2026-09-20 22:37:41 UTC (service start; 2026-09-21 04:07 IST; quiet-shop check clear: 0 orders in progress, 0 online in 60 min, newest order 408 min old; 77 clean minutes since Release 10) | `c220e38` (deployed detached) | `release/rc-25-rider` (fast-forwarded into `kit-radix-nova`) | `7bbae2c157e5cb22` | `0043_rider_assignment` (44 of 44 applied; head `created_at` 1790800000000; applied from the Mac, fail-fast, before the deploy) | Claude Code session (single builder), owner's 48-hour sprint rule (auth/permission card: security AND red-team agree, owner membership unchanged, sign-in works, immediate rollback on a failed smoke check) | Pass, signed out, GET only: `/` 200, `/menu` 200, `/sign-in` 200, `/cart` 200, `/robots.txt` 200, `/sitemap.xml` 200, `/api/health` 200, `/api/version` 200 (equals `.next/BUILD_ID`), `/order/not-a-uuid` 404, `/app/orders` `/app/deliveries` `/app/pos` `/app/staff` 307. Service active, 0 restarts, 0 journal lines at err, 0 nginx 5xx, 0 `Failed to find Server Action`, `systemctl --failed` empty. Owner checks, read-only: memberships fingerprint `aa1a9d898d44` identical before and after, both OWNER rows still active, GoTrue health via the public URL 200. Migration verified read-only: 2 columns, the check constraint, 108 existing orders untouched (0 with a rider) | Not run by me: signing in as anyone, assigning a rider, or closing a delivery (no order placed, nothing assigned); the rider screens rendered | Backup immediately before: `/var/backups/frybird/frybird-20260920-2236.dump`, 552,667 bytes. Contents: `orders.rider_id`; new permission `delivery.assign` (OWNER, ADMIN, MANAGER); assign a rider from the orders board and the Deliveries page; a rider sees and can close only deliveries assigned to them (an unassigned delivery is on nobody's rider list); 'Couldn't deliver' with a reason records FAILED (refused if any money was taken, guard inside advanceOrder's lock); the orders board page is now permission-gated (a RIDER could previously open /app/orders and read every open order with customer phones and addresses: a pre-existing gap found in the review, fixed in this release). Reviews: security (no blocker; delta re-review: both findings closed, full page/action sweep table), red-team (first review did not agree: H1 orders page, H2 paid order could end FAILED; both fixed fail-first; delta review: agree), payments (no veto; F1/F2 fixed), QA 7/7 on `c220e38` (unit 2878, integration 635/635 x3; a first QA run correctly failed 14 tests in two older delivery test files because rider actors closed unassigned deliveries: fixtures now assign the rider, assertions unchanged). Undo drilled on a real schema built from migrations 0000-0042 with a full fingerprint: restored exactly. Rollback: code-only is safe (the previous build ignores the columns; riders would see every delivery again); undo script `supabase/rollback/0043_rider_assignment.down.sql` drops the index, check and columns and loses only the assignments. BEHAVIOUR CHANGE AT THE DOOR: a RIDER now sees only deliveries assigned to them; until a manager assigns one, the rider's Deliveries list is empty |
| 16 | 2026-09-21 02:04:54 UTC (service start; 07:34 IST; quiet-shop check clear: 0 orders in progress, 0 online in 60 min, newest order 615 min old; 3h27 of clean journal since Release 11) | `ffa275d` (deployed detached) | `release/rc-24-batch` (fast-forwarded into `kit-radix-nova`) | `5aae9e01b7ce0a55` | `0044_customer_notes` through `0051_rls_read_new_tables` (52 of 52 applied; applied from the Mac, fail-fast, before the deploy) | Claude Code session (single builder), owner's 48-hour sprint rule (builders' batch; Paytm mock-only merge approved by the owner on four conditions, all verified) | Pass, signed out, GET only: `/` 200, `/menu` 200, `/sign-in` 200, `/cart` 200, `/robots.txt` 200, `/sitemap.xml` 200, `/api/health` 200, `/api/version` 200 (equals `.next/BUILD_ID`), `/order/not-a-uuid` 404, `/app/orders` `/app/kds` `/app/kds/expo` `/app/customers` `/app/staff/shifts` `/app/iq/kitchen` 307. Service active, 0 restarts, 0 journal lines at err, 0 nginx 5xx, 0 `Failed to find Server Action`, `systemctl --failed` empty. Owner checks, read-only: memberships fingerprint `aa1a9d898d44` identical, GoTrue health 200. Migration verified read-only: 5 new tables (all empty), `customers.notes`, 45 restrictive policies (40 + 5). Role simulation on production (counts only) unchanged from Release 10 | Not run by me: any screen rendered signed in (customer edit form, shifts and breaks, station screens, expo, PACK, kitchen analytics, promotions AOV), any order placed or moved, WhatsApp (off), Paytm (inert) | Backup immediately before: `/var/backups/frybird/frybird-20260921-0203.dump`, 554,499 bytes. Contents: kitchen prep targets (amber at 80%, red at 100% of `prep_minutes`), kitchen stations FRY/ASSEMBLY/DRINKS/PACK + EXPO (station = product override > category default > ASSEMBLY; sauces to PACK; combos by component, both FRY and ASSEMBLY if none defined; DRINKS tab hidden until a drink exists; marking a line done starts the order; PACK is the final step for takeaway/delivery), kitchen analytics, customer edit + notes, shifts (hours and breaks only, no pay logic, not tied to the till), promotions AOV, WhatsApp order-update outbox and mock provider (hook OFF unless `WHATSAPP_UPDATES=on`: no phone number is copied), Paytm mock-only provider (NOT VERIFIED AGAINST PAYTM SANDBOX; not selectable in production; checkout and payment methods unchanged; pinned by a test). RLS/PRIVILEGE (flagged): 0051 adds restrictive read policies for `notification_outbox` (phone numbers), `kitchen_line_status`, `kitchen_order_pack`, and own-rows-or-`staff.manage` for `shifts` and `shift_breaks`. Reviews: payments no veto (Paytm conditions verified in the merged code), security (two mediums fixed fail-first, delta re-review closed), red-team (first review did not agree: HIGH customer-edit idempotency key collided across page loads, fixed; MEDIUM READY reachable without stations = owner-visible advisory decision, HANDOVER 30; delta agree; a last look at the form's key mechanism: agree), QA 7/7 on `ffa275d` (unit 3070, integration 718/718 x2 and one known till flake that passes alone; two earlier QA runs correctly failed: 14 integration tests because the rider test fixtures were not merged, and 1 lint error: both fixed). Undo drilled on a real schema for all nine migrations 0043-0051 with a full fingerprint: restored exactly. Rollback: code-only is safe (the new tables and column are ignored by the previous build); undo scripts in `supabase/rollback/0044..0051` |
| 17 | 2026-09-21 12:42:13 UTC (service start; 18:12 IST; refined quiet-shop rule passed: 0 orders placed in the last 10 minutes, 0 in PENDING_PAYMENT under 15 minutes; one accepted takeaway idle 158 min did not block; 12 h of clean journal since Release 12) | `6235cc6` (deployed detached) | `release/rc-27-rider-offer` (merged into `kit-radix-nova`) | `6a0e5b7ae348f78a` | `0052_rider_rls_scope` (53 of 53 applied; head `created_at` 1791700000000; applied from the Mac, fail-fast, before the deploy) | Claude Code session (single builder), owner go/no-go YES (rider-rls-scope and rider-offer; RLS/PRIVILEGE change flagged) | Pass, signed out, GET only: `/` 200, `/menu` 200, `/sign-in` 200, `/cart` 200, `/robots.txt` 200, `/sitemap.xml` 200, `/api/health` 200, `/api/version` 200 (equals `.next/BUILD_ID`), `/order/not-a-uuid` 404, `/app/orders` `/app/deliveries` `/app/kds` `/app/staff` 307. Service active, 0 restarts, 0 journal lines at err, 0 nginx 5xx, 0 `Failed to find Server Action`, `systemctl --failed` empty. Owner checks, read-only: memberships fingerprint `aa1a9d898d44` identical, both OWNER rows active, GoTrue health 200. Migration verified read-only: helper function + 6 rider policies present, 51 restrictive policies total. Role simulation on production (counts only): OWNER 112 orders / 112 payments / 15 customers unchanged; MANAGER unchanged; CASHIER payments 0; KITCHEN payments 0 customers 0; RIDER orders 0, order_items 0, order_events 0 (BEFORE this release a rider read all 108 orders) | Not run by me: any screen rendered signed in (rider Deliveries with Available offers, Take it), nothing assigned, no order placed | Backup immediately before: `/var/backups/frybird/frybird-20260921-1240.dump`, 582,630 bytes. Contents: rider-rls-scope (RLS/PRIVILEGE: a rider-only login reads only its own assigned deliveries and their items, modifiers, events and kitchen rows at the database level; helper `auth_is_rider_scoped` + six restrictive policies) and rider-offer (unassigned READY/OUT_FOR_DELIVERY deliveries show to every active rider as Available with pickup-level facts only; Take it is atomic, first tap wins; new permission `delivery.take` for RIDER; rider screen polls every 10 s). Reviews: security approve, red-team agree, QA 7/7 on `6235cc6` (unit 3080, integration 738/738 x3; an earlier QA run correctly failed one older test that expected a rider to see an unassigned order's events, expectation updated). Undo drilled on a real schema built from 0000-0051: restored exactly. Rollback: code-only is safe (the code never depends on the policies); undo script `supabase/rollback/0052_rider_rls_scope.down.sql` drops the 6 policies and the function, no data |
| 18 | 2026-09-21 13:15:31 UTC (service start; 18:45 IST; refined quiet-shop rule passed: 0 orders placed in the last 10 minutes, none in PENDING_PAYMENT, no open orders at all; 32 clean minutes since Release 13) | `b9e860a` (deployed detached) | `release/rc-30-rider-followups` (merged into `kit-radix-nova`) | `e437298b6318add8` | none (`0052_rider_rls_scope` unchanged; 53 of 53) | Claude Code session (single builder), owner's sprint rule (code-only permission/staff-side release: security and red-team agree, QA green on the exact commit, no migration) | Pass, signed out, GET only: `/` 200, `/menu` 200, `/sign-in` 200, `/cart` 200, `/checkout` 307 to `/cart` (empty cart: existing behaviour), `/robots.txt` 200, `/sitemap.xml` 200, `/api/health` 200, `/api/version` 200 (equals `.next/BUILD_ID`), `/order/not-a-uuid` 404, `/app/orders` `/app/deliveries` `/app/kds` `/app/staff` 307. Service active, 0 restarts, 0 journal lines at err, 0 nginx 5xx, 0 `Failed to find Server Action`, `systemctl --failed` empty. Owner checks, read-only: memberships fingerprint `aa1a9d898d44` identical, both OWNER rows active, GoTrue health 200. `COOKIE_SECRET` is NOT set on the server (0 lines): cookie behaviour is unchanged until the owner-approved step (DEPLOY 12) | Not run by me: any screen rendered signed in (Release button, at-limit state, the stale flag), any order placed | Backup immediately before: `/var/backups/frybird/frybird-20260921-1314.dump`, 587,632 bytes. Contents: rider-take-cap (a rider holds at most 2 active deliveries; atomic per rider; audited Release only while READY; at most 6 takes per rolling hour so take/release cannot read every customer's details), rider-stale-hold (flag only, 15 minutes, on the orders board and Deliveries page for managers), cookie-sign-1 (with COOKIE_SECRET set the `frybird_contact` cookie is signed and carries the orders this browser placed; ownership of an order is decided by that list, never by a phone number; a malformed secret fails closed; unset = unchanged; the cookie now carries the Secure flag in production). Reviews: security (cookie rework: pass; follow-ups: no blocker), red-team (cookie rework: agree after the first review found that a signed cookie could still be minted for a typed phone number, fixed by binding to order ids; follow-ups: first review did not agree, fixes: Release READY-only and 6 takes per hour, delta agree), QA 7/7 on `b9e860a` (unit 3109, integration 752/752 x3). Rollback: code-only, redeploy `6235cc6` (BUILD_ID `6a0e5b7ae348f78a`); no migration |
| 19 | 2026-09-21 21:11:29 UTC (service start; 2026-09-22 02:41 IST; refined quiet-shop rule passed at 19:05 IST: 0 orders placed in the last 10 minutes, none in PENDING_PAYMENT, no open orders; deployed by the owner from a separate Terminal after the auto-mode classifier denied the deploy command in the Claude session) | `efc5ee5` (deployed detached; `DEPLOY_COMMIT` on the service equals it; code identical to `bec752f` apart from two test files and docs) | `release/rc-28-till-fix` (merged into `kit-radix-nova`) | `bab6ef62ec92de90` | `0053_refund_cash_session` (54 of 54 applied from the Mac at ~19:10 IST, before the deploy; additive, undo drilled) | Claude Code session (integrator); deploy run by the owner | Pass, signed out, GET only: `/` 200, `/menu` 200, `/sign-in` 200, `/cart` 200, `/robots.txt` 200, `/sitemap.xml` 200, `/api/health` 200, `/api/version` = BUILD_ID, `/order/not-a-uuid` 404, `/app/orders` `/app/deliveries` `/app/kds` `/app/staff` `/app/finance` 307. Service active, 0 restarts, 0 nginx 5xx in the last 15 min, `systemctl --failed` empty; 4 `Failed to find Server Action` lines from tabs open on the old build (expected once after a deploy). Owner checks, read-only: memberships fingerprint `aa1a9d898d44` identical, both OWNER rows active. `COOKIE_SECRET` still NOT set (0 lines) | Not run by me: a signed-in session (no credentials; no order, payment or till action taken in production) | Owner YES with six conditions, all met: payments reviewer no veto on exactly `bec752f` (9 tool uses); till-close-micros root cause proven fail-first (old time-window query gave expected 150000n against 138000n; new code passes); rider-held and earlier-session refund tests show variance 0; unopened till blocks nothing (tests); 0053 undo drilled on 0000-0052 (fingerprint identical, re-apply identical); code-only rollback safe (old code never names the column). Backup: `/var/backups/frybird/frybird-20260921-1335.dump`. Removed from the till go-live checklist: `cash-refund-attribution`, `till-close-micros`. |
| 19a | 2026-09-21 21:41 UTC (restart; 03:11 IST; 30 clean minutes after Release 15, shop quiet: 0 orders in the last 10 minutes) | no code change (`efc5ee5` unchanged) | n/a | `bab6ef62ec92de90` (unchanged) | none | Claude Code session (integrator), owner-approved | Pass: service active, 0 restarts, 0 journal errors, `systemctl --failed` empty; `/`, `/menu`, `/sign-in`, `/cart` 200; `/checkout` 307 to `/cart` and `/order/<unknown id>` 404 (same as before); `/app/orders` 307 to sign-in | Owner had confirmed sign-in on Release 15; memberships fingerprint `aa1a9d898d44` unchanged after the restart | **`COOKIE_SECRET` is now SET** (server-generated with `openssl rand -hex 32`, appended to `/etc/frybird/env`, never printed; 64 hex characters, one line). Backup of the previous env: `/etc/frybird/env.bak-cookie-202609212141` (mode 640, root:frybird). Effect: contact cookies issued before this are unsigned, so **returning customers re-enter their contact once**; new cookies are signed and bound to the order ids. Rollback if ever needed: copy the backup over `/etc/frybird/env` and `systemctl restart frybird`. |
| 20 | 2026-09-21 22:35:20 UTC (service start; 2026-09-22 04:05 IST; refined quiet-shop rule passed at 04:03 IST: 0 orders placed in the last 10 minutes, 0 in PENDING_PAYMENT, no open orders) | `f0639e0` (deployed detached) | `release/rc-32-rider-limits-admin` (fast-forwarded into `kit-radix-nova`) | `1efb072e49959f4b` | `0054_rider_limits` (55 of 55 applied from the Mac, fail-fast, before the deploy; additive, undo drilled on a schema built from 0000-0053, fingerprint restored exactly, re-apply identical) | Claude Code session (integrator), owner's sprint rule (settings UI plus additive migration; not a payments/till/secret/data change) | Pass, signed out, GET only: `/` `/menu` `/sign-in` `/cart` `/robots.txt` `/sitemap.xml` `/api/health` 200, `/order/not-a-uuid` 404, `/app/orders` `/app/deliveries` `/app/kds` `/app/staff` `/app/admin/restaurant` 307, `/api/version` = BUILD_ID. Service active, 0 restarts, 0 journal errors, 0 nginx 5xx in 15 min, `systemctl --failed` empty, `COOKIE_SECRET` still set. Owner checks, read-only: memberships fingerprint `2e1c2fd91279` (the new baseline) identical, 1 OWNER + 1 RIDER active. Migration verified read-only: both columns NOT NULL with defaults 2 and 6, both CHECKs present, the shop reads 2/6 | Not run by me: signing in, editing the limits in Admin, a rider taking a delivery (no order placed, nothing written) | Backup immediately before: `/var/backups/frybird/frybird-20260921-2233.dump`, 592,966 bytes. Contents: rider-limits-admin: Admin > Restaurant > Riders lets the OWNER set deliveries a rider can hold at once (1-5, default 2) and takes per hour (1-20, default 6); read inside the take transaction so a change applies to the next Take it; audited (`rider_limits_changed`, old and new values); the database CHECKs hold the bounds even against a hand edit, so the take cap cannot be switched off. RLS/PRIVILEGE (flagged): none; the two new columns are readable by shop members like the rest of `organizations`, writes stay owner-only through the action (client roles cannot write, 0033). Reviews: security approve (5 tool calls), red-team agree (7 tool calls, no High/Medium), QA on `f0639e0` (unit 3125, integration 777/777 x3, lint 0 errors, typecheck clean). Rollback: code-only is safe (the previous build ignores the columns and uses 2 and 6); undo `supabase/rollback/0054_rider_limits.down.sql` |
| 21 | 2026-09-21 23:13:40 UTC (service start; 2026-09-22 04:43 IST; refined quiet-shop rule passed at 04:42 IST: 0 orders placed in the last 10 minutes, 0 in PENDING_PAYMENT, no open orders; owner YES with a five-point self-check, all met) | `2eb78f0` (deployed detached; till commit `7111a7f` merged with the live `f0639e0`) | `release/rc-31-till-golive` (fast-forwarded into `kit-radix-nova`) | `a8971b211203c5ad` | none (55 of 55 unchanged) | Claude Code session (integrator); owner go/no-go YES (till code is a gated category) | Pass, signed out, GET only: `/` `/menu` `/sign-in` `/cart` `/robots.txt` `/sitemap.xml` `/api/health` 200, `/order/not-a-uuid` 404, `/app/orders` `/app/deliveries` `/app/kds` `/app/staff` `/app/finance` 307, `/api/version` = BUILD_ID. Service active, 0 restarts, 0 journal errors, `systemctl --failed` empty; 2 `Failed to find Server Action` lines from tabs open on the old build (expected once after a deploy). Owner checks, read-only: memberships fingerprint `2e1c2fd91279` identical, 1 OWNER + 1 RIDER active | Not run by me: any signed-in screen (the Till panel, the blind Finance figures, a handover), no till opened, no order, payment or refund | Backup immediately before: `/var/backups/frybird/frybird-20260921-2312.dump`, 593,487 bytes. Contents: `withIdempotency` on till open, close and handover (a double-tap returns the first result; a key reused with different content is refused); `refund-audit-till` (the `payment_refunded` audit row of a cash refund carries `cashSessionId`, null when no till was open); blind count enforced on the cash figures (reconciliation withholds till cash, cash refunds and net for days since the open till started; Finance withholds the Cash KPI, cash card/chart and the By-method cash row); the handover form states the amount owed; runbook DEPLOY.md 13. Blind close on the finance lists is left as is by the owner (only OWNER has finance access; lists keep showing cash for reconciliation): low card `blind-close-finance-lists`. Reviews: payments no veto (4 tool calls), security approve (7), QA gates on `7111a7f` by the QA agent and re-run by me on `2eb78f0`: unit 3134, integration 787/787, lint 0 errors, typecheck clean; the build ran inside deploy.sh. No RLS/privilege change, no migration. Rollback: code-only, redeploy `f0639e0` (BUILD_ID `1efb072e49959f4b`); no schema change |
| 22 | 2026-09-21 23:23:39 UTC (service start; 2026-09-22 04:53 IST; refined quiet-shop rule passed at 04:52 IST: 0 orders placed in the last 10 minutes, 0 in PENDING_PAYMENT) | `a9025c9` (deployed detached) | `release/rc-33-lows` (fast-forwarded into `kit-radix-nova`) | `bbb5ae2c42d8a94d` | none (55 of 55 unchanged) | Claude Code session (integrator), owner's overnight order (lows that touch no money, order-placement, auth or migration code, sprint rules) | Pass, signed out, GET only: `/` `/menu` `/sign-in` `/cart` `/api/health` 200, `/order/not-a-uuid` 404, `/app/orders` `/app/iq` `/app/staff/shifts` 307, `/api/version` = BUILD_ID. Service active, 0 restarts, 0 journal errors, `systemctl --failed` empty. Owner checks, read-only: memberships fingerprint `2e1c2fd91279` identical | Not run by me: any signed-in screen (the Overview readiness fallback, clocking out) | Backup immediately before: `/var/backups/frybird/frybird-20260921-2322.dump`, 593,569 bytes. Contents: `readiness-safe-1` (the Overview no longer fails if the readiness read throws: the panel becomes a note, the cards that lean on it show as limited, the error name is logged); `clockout-same-ms` (a clock-out in the same millisecond as, or before, the clock-in closes the shift 1 ms after it began instead of failing the table check; the break end and audit use the stored time); `iq-flaky-1` (the race test waits until the second transaction is actually blocked on a lock); `flaky-s4` (test timeout 30 s to 90 s); `secure-cookie-note` (DEPLOY.md). Reviews: security approve (3 tool calls; one low taken: log the error). QA by me: typecheck clean, lint 0 errors, unit 3134, integration 789/789 x3. No RLS/privilege change, no migration. Rollback: code-only, redeploy `2eb78f0` (BUILD_ID `a8971b211203c5ad`) |
| 23 | 2026-09-22 00:23:23 UTC (service start; 05:53 IST; refined quiet-shop rule passed at 05:51 IST: 0 orders placed in the last 10 minutes, 0 in PENDING_PAYMENT, no open orders) | `bd82f13` (deployed detached) | `release/rc-35-rider-live` (built on `release/rc-34-security-lows`; fast-forwarded into `kit-radix-nova`) | `1b5396427947afbb` | `0055_client_grant_hardening`, `0056_rider_positions` (57 of 57 applied from the Mac, fail-fast, before the deploy; both additive/grant-only, undo drilled on schemas built from 0000-0054 and 0000-0055 with RLS/grants in the fingerprint: exact, re-apply identical) | Claude Code session (integrator), owner go/no-go YES on both (rc-34: held-back security/robustness lows, self-check passed; rc-35: rider live position, customer-facing, three conditions confirmed) | Pass, signed out, GET only: `/` `/menu` `/sign-in` `/cart` `/robots.txt` `/sitemap.xml` `/api/health` 200, `/order/not-a-uuid` 404, `/api/order/<unknown-uuid>/rider` 404, `/app/orders` `/app/deliveries` `/app/kds` `/app/staff` 307, `/api/version` = BUILD_ID. Service active, 0 restarts after startup, `systemctl --failed` empty. One restart cycle and a handful of `Failed to find Server Action`/stale-tab `NotSignedIn` lines at the moment of the swap (expected once per deploy, self-clears). Owner checks, read-only: memberships fingerprint `2e1c2fd91279` identical, 1 OWNER + 1 RIDER active | Not run by me: any signed-in screen (a rider's Share-location prompt, the customer's live map), no delivery taken or GPS fix posted | Backup immediately before: `/var/backups/frybird/frybird-20260922-0021.dump`, 593,688 bytes, 80 tables. **rc-34 (held-back security/robustness lows, HANDOVER §37):** `invite-race-1` (invite is insert-first, the unique index arbitrates, the loser resends), `staff-lock-1` (deactivate/role-change lock the target's rows, in id order, before deciding the ceiling), `reassign-race-1`/`complete-release-race` (a rider's delivery close and cash capture re-check who holds it under the order's row lock; `failDelivery` too), `ready-gate-enforce` (the Orders/KDS board's "Ready" refuses open station tasks once a shop uses the station screens; an order no station has touched is unaffected), station done/undo/pack audit rows, `outbox-paid-position` (WhatsApp order-update messages read payment state through the same rules as the kitchen guard). **PRIVILEGE/RLS CHANGE (0055, flagged):** `anon` loses SELECT on `memberships` and `orders`; `authenticated` loses direct PostgREST read of `orders`' customer contact columns (name, phone, delivery address, lat/lng, notes). Nothing in the app reads either table through PostgREST (verified by search and by test), so no client path is affected. **rc-35 (Lane B, rider live position):** the rider's browser posts a GPS fix every ~15 s while a delivery is OUT_FOR_DELIVERY (`delivery.complete`, rider identity always server-derived); the customer's `/order/[id]` page shows the newest fix and the destination on a Leaflet/OSM map, position-only (no name or phone), only to the order's own owner, only while the order is out; fixes are deleted after 24 hours by the new `rider-positions-purge` job (installed and enabled on this release, verified with one manual run: SUCCEEDED). New table `rider_positions`: RLS forced, ALL privileges revoked from `PUBLIC`/`anon`/`authenticated`, no client policy at all — unreachable through PostgREST/Realtime, verified read-only. Reviews: rc-34 — security APPROVE, payments NO VETO, red-team AGREE (two findings fixed live: a rider could fail a reassigned delivery, one refusal branched on wording instead of a code); rc-35 — security APPROVE, red-team AGREE (one pre-existing, currently-inert low carried forward: the legacy unsigned remembered-contact cookie, dormant while `COOKIE_SECRET` stays set, would also expose live location if that secret were ever unset — card `cookie-secret-dependency`, owner has asked for it removed next release). QA by me on `bd82f13`: typecheck clean, lint 0 errors, unit 3173, integration 831. Rollback: code-only, redeploy `a9025c9` (BUILD_ID `bbb5ae2c42d8a94d`); both migrations are additive/grant-only and their undos are drilled, but a code-only rollback needs no undo since the old code never reads the new table or the revoked columns |
| 24 | 2026-09-22 23:23:09 UTC (service start; 04:53 IST; quiet-shop check passed: 0 orders placed in the last 10 minutes, 0 in PENDING_PAYMENT) | `715308e` (merged into `kit-radix-nova`) | `release/rc-37-rider-pwa-health` (fast-forwarded into `kit-radix-nova`) | `1d1147b27c8b6e19` | unchanged (57 of 57; no new migration) | Claude Code session (single builder), owner's sprint rule (UI-only, staff-side, no gate needed; shipped autonomously after one review) | Pass, signed out, GET only: `/` `/menu` `/sign-in` `/cart` `/robots.txt` `/sitemap.xml` `/api/health` 200, `/order/not-a-uuid` 404, `/app/orders` `/app/deliveries` `/app/kds` `/app/staff` `/app/iq/live` `/app/iq` 307, `/api/version` = BUILD_ID. Service active, 0 restarts, 0 journal errors, `systemctl --failed` empty. Smoke verifications on the live site (POST deploy, GET only): `/manifest.webmanifest` 200 (correct JSON with startUrl /app), `/apple-icon.png` 200 (180x180), `/pwa/icon-192.png` 200, `/pwa/icon-maskable-512.png` 200; `/app/iq/live` and `/app/deliveries` both correctly redirect 307 to sign-in when unauthenticated | Not run by me: the Install button rendered on Chrome/Android, the iOS manual "Add to Home Screen" explanation, the Order Health strip rendered on signed-in staff screens (`/app/iq/live` and `/app/iq`), the screen wake lock on active deliveries (pre-existing from Release 23, not added here) | Backup immediately before: `/var/backups/frybird/frybird-20260922-2318.dump`, 593,688 bytes, 81 tables. **Rider PWA (roadmap 4.3, part 1):** `src/app/manifest.ts` (Next.js file convention, `start_url=/app scope=/app` so installed icon opens staff area, never customer site), icons under `public/pwa/` (192/512 raster, "any" + "maskable" purposes from existing `src/app/icon.svg`), `src/app/apple-icon.png` (180x180 for iOS home screen), new `src/components/staff/install-app-prompt.tsx` (captures `beforeinstallprompt`, offers button on Chrome/Android, explains iOS "Share → Add to Home Screen" on iOS where no programmatic install exists; dismiss state persists in `localStorage` but cancelling the native dialog does not set the same flag as explicit "Not now", cosmetic UX-only note, not a blocker; shown only on rider's own Deliveries screen via `riderOnly` / `seesOnlyOwnDeliveries` check). Screen wake lock while OUT_FOR_DELIVERY already existed from Release 23's `ShareLocation` component, not added here. **Order Health strip (roadmap 4.3, part 2):** pure `healthCounts()` in `src/lib/kitchen/tickets.ts` (built on existing roadmap-4.1 `prepHealth()` rule; GREEN/AMBER/RED counts at 80%/100% prep-minutes thresholds), new `src/components/iq/order-health-strip.tsx` rendering them. Wired into `/app/iq/live` (previously called `toKitchenTickets` with no prep targets at all, now passes real targets from `getPrepTargets`, fixing the "every ticket was only GREEN or RED, never AMBER" gap) and into `/app/iq` Command Center page's existing "Order health right now" card (reuses `activeOrders` query already run there, one new `getPrepTargets` call, no extra order-list query). `getPrepTargets` org-scoping verified correct at both new call sites (`staff.orgId` throughout, ids drawn only from that org's own active orders). **Review:** frybird-security agent (22 tool calls, real use) APPROVE verdict. Confirmed: no server-side data write except client-local `localStorage` dismiss flag in install prompt; `riderOnly` gating reuses page's existing `seesOnlyOwnDeliveries` check, not new; manifest `/app` scope changes no security boundary (same `getStaff()` / `staffCan()` auth gate in layout applies to installed PWA exactly as to browser tab); install-prompt state transitions only reflect real browser events (`appinstalled`, `userChoice.outcome === "accepted"`), no false success path. Gates: typecheck clean, lint 0 errors, unit 3177, integration 833, all green. No follow-up card needed. **Rollback:** code-only, redeploy `bd82f13` (BUILD_ID `1b5396427947afbb`); safe at any time (no migration, no schema change, no auth/permission change, no server data write) |
| 25 | 2026-09-22 23:42:03 UTC (service start; quiet-shop check passed at 23:38: 0 orders placed in the last 10 minutes, 0 in PENDING_PAYMENT) | `5782220` (merge commit `Merge release/rc-36-cookie-secret-dependency: cookie-secret-dependency` onto `kit-radix-nova`'s current tip, which carried Release 24) | `release/rc-36-cookie-secret-dependency` (based on `kit-radix-nova` at `39ff19d`, the same base commit as rc-37, before rc-37 existed — a sibling branch, not stacked; merged onto live `kit-radix-nova` to keep Release 24) | `e2260caa445edaf4` | unchanged (57 of 57; no new migration) | Claude Code session (integrator), owner-approved on `65bfab7` (security APPROVE, red-team AGREE); gates re-verified green on the merge commit `5782220` | Pass, signed out, GET only: `/` `/menu` `/sign-in` `/cart` `/robots.txt` `/sitemap.xml` `/api/health` 200, `/order/not-a-uuid` 404, `/app/orders` `/app/deliveries` `/app/kds` `/app/staff` `/app/iq/live` `/app/iq` 307, `/api/version` = BUILD_ID. Service active, 0 restarts, 0 journal errors, `systemctl --failed` empty | Not run by me: signed-in screens or order tracking; the cookie behaviour change is invisible to unauthenticated users | Backup immediately before: `/var/backups/frybird/frybird-20260922-2338.dump`, 81 tables. **Gated auth release:** removes the unsigned remembered-contact cookie fallback entirely; if `COOKIE_SECRET` is unset or the value fails to validate, the `frybird_contact` cookie is now treated as absent (no read, no write) and an alert is logged, instead of falling back to trusting a bare phone number. `65bfab7` approved 2026-09-21; the branch was based on `kit-radix-nova` at `39ff19d` before rc-37 existed, so deployment needed a merge onto the live code to preserve Release 24. Gates on `5782220`: typecheck clean, lint 0 errors (19 pre-existing unrelated warnings), 192 unit test files / 3180 tests all green, 82 integration test files / 833 tests all green. No rollback needed. **Follow-up card `cookie-secret-dependency` (DONE — shipped in Release 25):** removes the legacy dormant fallback as the owner requested. |
| 26 | 2026-09-23 11:17:20 UTC (service start; 16:47 IST; quiet-shop check passed ~11:13 UTC: 0 orders placed in the last 10 minutes, 0 in PENDING_PAYMENT) | `1c7a5e6` (merge commit onto `kit-radix-nova`'s tip, which carried Release 25) | `release/rc-38-analytics-start-date` (built `8a21b2e`, fixed to `0417a40` after the architect review below, merged onto live `kit-radix-nova`) | `866c6a9dc872486d` | unchanged (57 of 57; no new migration — `organizations.opened_on` already existed, the toggle is a URL param) | Claude Code session (integrator), owner-approved: run security + architect on `8a21b2e` with real tool use; if both clean, merge and deploy under standing approval | Pass, signed out, GET only: `/` `/menu` `/sign-in` `/cart` `/robots.txt` `/sitemap.xml` `/api/health` `/manifest.webmanifest` `/apple-icon.png` 200, `/order/not-a-uuid` 404, `/app/orders` `/app/deliveries` `/app/kds` `/app/staff` `/app/iq/live` `/app/iq` `/app/iq/pnl` `/app/iq/brief` 307. Service active, 0 restarts, 0 journal errors (the usual SIGTERM/exit-143 restart line only), `systemctl --failed` empty | Not run by me: any signed-in screen — the Command Center's pre-launch toggle, the P&L page's toggle link, the daily brief | Backup immediately before: `/var/backups/frybird/frybird-20260923-1113.dump`, 81 tables. **`analytics-start-date`:** data recorded before `organizations.opened_on` is excluded by default from the daily brief, P&L comparisons, readiness scores and IQ detectors, with a visible "include pre-launch data" toggle on the Command Center and the P&L page; Finance totals, the payments ledger, GST invoices and CSV/report exports are untouched — confirmed by both reviewers, byte-for-byte, that `src/lib/repositories/finance.ts`, `expenses.ts`'s `getProfitAndLoss`/`getProfitAndLossReport` themselves, and `/api/reports/export` carry zero diff; every clamp is applied at page-level call sites only. New pure module `src/lib/iq/launch-window.ts` (`isPreLaunch`, `clampSpanToLaunch`, `clampRangeToLaunch`, `preLaunchDates`, 20 unit tests). Readiness (`iq-readiness.ts`): every window clamped except `stockCount` (inventory-count hygiene, not an order trend). Detectors (`detect.ts`, `pulse.ts`): `excludedDates` (previously hardcoded `[]`) now also carries pre-launch dates via a new `JobReadRepos.readOpenedOn()`, unconditional (no viewer to toggle). The brief (`brief-job.ts`): `briefPeriods` clamps month spans; a separate guard skips all five day-window facts (Net sales, Orders, Average order, Food cost, Collected) when the day itself is pre-launch — added after the architect review found this exact gap (see below). Command Center + P&L: `getRangeComparison` clamps the current window and, after the fix below, checks every comparison-baseline range directly; the Channel performance, Payment methods, top-selling-products and not-selling cards on the Command Center read the same clamped window as the KPI tiles. **Review history — two rounds:** round 1 on `8a21b2e`: security APPROVE (44 tool calls, checked out the branch, ran every gate itself, traced the Finance/exports invariant and the comparison-baseline safety claim analytically); architect **BLOCK** (46 tool calls) with two HIGH/HIGH-MEDIUM findings, both demonstrated with real reproductions against the actual code (not disputed): (1) the comparison baseline's own "no separate clamp needed" claim was false — `compareOptions` gates comparison options by counting history from `today`, but for `range !== "today"` the real reference day is one day earlier, an off-by-one that let an "available" comparison still reach one day before the Opening date; (2) the Channel performance/Payment methods/top-products/not-selling cards read the raw unclamped window, inconsistent with the clamped KPI tiles beside them — a starker gap than the trend-chart scope-out the original commit disclosed. Plus two lower-severity findings: (3) the brief's day-window facts had no pre-launch guard at all (only the month spans did); (4) the daily-brief page's readiness call had no toggle, undocumented, unlike the Command Center's. Fixed all four in commit `0417a40` (finding 5, two near-identical `opened_on` readers in `iq-job-runs.ts`, left as an acknowledged follow-up per the reviewer's own recommendation) — added `src/lib/repositories/overview.integration.test.ts` (new file, 5 tests against the real DB, reproduces the reviewer's exact failing scenario against the pre-fix code shape to prove it's a real regression test) and 2 new tests in `brief-job.test.ts`. Round 2 on `0417a40`: architect **APPROVE** (41 tool calls) — independently re-verified all four fixes line-by-line, re-ran the new tests itself against the real database, confirmed the follow-up (finding 5) is still just that. Gates on the final merge commit `1c7a5e6`: typecheck clean, lint 0 errors (19 pre-existing unrelated warnings), 193 unit test files / 3208 tests, 83 integration test files / 843 tests, `check:rsc-boundaries` clean. No rollback needed; nothing here can be rolled back by code anyway, since it's additive analytics logic with no migration. **Fingerprint anomaly, unrelated to this release, flagged to the owner (push sent):** the post-deploy fingerprint read `4a3d000a67a52b7e857e320f2d79df2b`, not the standing baseline `2e1c2fd91279...`. Read the full `memberships` table read-only: a new ACTIVE OWNER membership (`c47df963-aadb-4555-bf87-2ee9d98448c7`) exists, created `2026-09-23 10:13:36 UTC` — before this deploy's quiet-shop check (~11:13 UTC) and before the fresh dump, and long before any file in this release's diff was written. This release contains zero membership-table code (confirmed by both reviewers finding no auth/permission changes at all), so this is a real, unrelated production state change this routine check happened to catch, not something this deploy caused or something a rollback would undo. Not treated as a failed smoke (the app itself is fully healthy); owner notified to confirm the new OWNER account is expected. The new fingerprint is NOT yet adopted as the standing baseline pending that confirmation. |
| 27 | 2026-09-24 19:42:55 UTC (service restart; quiet-shop check passed ~19:38 UTC: 0 orders placed in the last 10 minutes, 0 in PENDING_PAYMENT) | `42727a89f03ebc3ecb6843a87f5abecc81c12f50` | `kit-radix-nova` | `4bb0672ad7333701` | unchanged (57 of 57; no new migration) | Claude Code session (single builder), standing approval (UI-only asset, no migration) | Pass, signed out, GET only: `/` `/menu` `/sign-in` `/cart` `/robots.txt` `/sitemap.xml` `/api/health` `/manifest.webmanifest` `/apple-icon.png` 200, `/order/not-a-uuid` 404, `/app/orders` `/app/deliveries` `/app/kds` `/app/staff` `/app/iq/live` `/app/iq` `/app/iq/pnl` `/app/iq/brief` 307. `/api/version` on the VPS returns `{"build":"4bb0672ad7333701"}`, matching the local `.next/BUILD_ID`. `/email/frybird-logo.png`: 200, `Content-Type: image/png`, `Content-Length: 13511`, `Cache-Control: public, max-age=2592000, stale-while-revalidate=2592000` (matches `next.config.ts`'s new `/email/:path*` header exactly); downloaded copy is SHA-256 byte-identical to the local file and visually confirmed as the FRYBIRD wordmark. Service active, 0 restarts, 0 journal errors, `systemctl --failed` empty | Not run: nothing gated behind a session — this release has no signed-in surface (an asset URL and a `next.config.ts` header only) | Backup immediately before: `/var/backups/frybird/frybird-20260924-1941.dump`, 81 tables. Post-deploy fingerprint re-checked read-only: `4a3d000a67a52b7e857e320f2d79df2b`, 2 active OWNER / 1 active RIDER — matches the baseline adopted in HANDOVER 44, no anomaly. **Content of this release:** `public/email/frybird-logo.png` (new, 240×59, 13.5 KB) — the receipt wordmark `public/frybird-receipt-logo.png` (existing, brand-approved, already in production use on the printed receipt) resized via `sips`, served at `https://frybirdiq.tech/email/frybird-logo.png`. `next.config.ts`'s `headers()` gained a `/email/:path*` route at a 30-day cache (`max-age=2592000`), chosen over `immutable` since the filename is not content-hashed. `docs/email-templates/login.html` (new, not app code, not served by this app) — the table-based, inline-styled Supabase Magic Link / Confirm signup email template using `{{ .Token }}` and `{{ .ConfirmationURL }}`, referencing the new logo URL, for the owner to paste into the Supabase dashboard by hand. Originally requested as a copy of `~/Downloads/frybird-logo.gif`, which did not exist; owner redirected to this PNG-from-existing-asset approach mid-card (see HANDOVER). |
| 28 | 2026-09-24 20:07:40 UTC (service start; quiet-shop check passed ~20:06 UTC: 0 orders placed in the last 10 minutes, 0 in PENDING_PAYMENT) | `488732e` | `kit-radix-nova` | `a55b910e93d3e4dd` | unchanged (57 of 57; no new migration) | Claude Code session (single builder), standing approval (UI-only asset, no migration) | Pass, signed out, GET only: `/` `/menu` `/sign-in` `/cart` `/robots.txt` `/sitemap.xml` `/api/health` `/manifest.webmanifest` `/apple-icon.png` 200, `/order/not-a-uuid` 404, `/app/orders` `/app/deliveries` `/app/kds` `/app/staff` `/app/iq/live` `/app/iq` `/app/iq/pnl` `/app/iq/brief` 307. `/api/version` matches local BUILD_ID. `/email/frybird-logo.gif`: 200, `Content-Type: image/gif`, `Content-Length: 41858`, `Cache-Control: public, max-age=2592000, stale-while-revalidate=2592000` (already covered by the existing `/email/:path*` rule, no config change needed); downloaded copy is SHA-256 byte-identical to the local file and visually confirmed as the FRYBIRD wordmark. Service active, 0 restarts, 0 journal errors, `systemctl --failed` empty | Not run: nothing gated behind a session — this release has no signed-in surface (an asset URL and a documentation-only template file) | Backup immediately before: `/var/backups/frybird/frybird-20260924-2006.dump`, 81 tables. Post-deploy fingerprint re-checked read-only: `4a3d000a67a52b7e857e320f2d79df2b`, 2 active OWNER — matches the HANDOVER 44 baseline, no anomaly. **Content of this release:** `public/email/frybird-logo.gif` (new, 400×92, 41 KB) replaces the originally-requested-but-missing file from two cards ago, now supplied by the owner; served at `https://frybirdiq.tech/email/frybird-logo.gif`. `public/email/frybird-logo.png` (from Release 27) is unchanged and kept in place, unreferenced by this template. `docs/email-templates/login.html` (not app code, not served by this app) replaced end-to-end with the owner-supplied final design, referencing the new GIF URL; `{{ .Token }}` and `{{ .ConfirmationURL }}` preserved exactly, for the owner to paste into the Supabase dashboard by hand. |
| 29 | 2026-09-24 21:20:12 UTC (service start; quiet-shop check passed ~21:18 UTC: 0 orders placed in the last 10 minutes, 0 in PENDING_PAYMENT) | `21b549d` (merge commit `Merge release/rc-41-email-otp` onto `kit-radix-nova`'s tip, which also carried the nginx `proxy_buffer_size` incident fix from `9b4d129` — that fix was already live via a direct reload before this deploy and is unrelated to this card's own content; see HANDOVER §45 for the incident, a separate §45 on the rc-41 branch for this card) | `kit-radix-nova` (from `release/rc-41-email-otp` @ `c5eeaf8`) | `7c2ce9dc7dcf2366` | unchanged (57 of 57; no new migration) | Claude Code session (integrator), owner go/no-go YES 2026-09-24 after answering 3 required verification questions: (1) a verify-while-signed-in-as-another-account fully replaces the session, traced through `@supabase/ssr`'s actual `applyServerStorage` cookie-chunk-clearing code, not assumed; (2) code length (6) matches `supabase/config.toml`'s `otp_length` and the owner's live Dashboard config (Site URL, redirect wildcard, templates, 600s expiry, custom SMTP), with one Dashboard-side item flagged for the owner to confirm (the project's emails-per-hour rate limit, unrelated to app code); (3) staff/owner password sign-in confirmed zero diff across all three of its files | Pass, signed out, GET only: `/` `/menu` `/sign-in` `/cart` `/robots.txt` `/sitemap.xml` `/api/health` `/manifest.webmanifest` `/apple-icon.png` `/account/sign-in` 200, `/order/not-a-uuid` 404, `/app/orders` `/app/deliveries` `/app/kds` `/app/staff` `/app/iq/live` `/app/iq` `/app/iq/pnl` `/app/iq/brief` 307. Local `.next/BUILD_ID` matches the deployed one exactly. Service active, 0 restarts, 0 journal errors, `systemctl --failed` empty, no new nginx errors since the deploy | Not run by me: the OTP flow itself (typing/pasting a real code, the magic-link fallback) — the owner is testing live per their own instruction ("I will test live") | Backup immediately before: `/var/backups/frybird/frybird-20260924-2118.dump`, 81 tables. Post-deploy fingerprint re-checked read-only: `4a3d000a67a52b7e857e320f2d79df2b`, 2 active OWNER — matches baseline, no anomaly. **Content of this release (`email-otp` card):** customer sign-in (`/account/sign-in`) now sends a 6-digit code by email (`signInWithOtp`, `shouldCreateUser:false`) instead of a password, verified via `verifyOtp({email,token,type:"email"})` in new segmented auto-submitting code boxes (`src/components/account/code-boxes.tsx`); the magic link stays working as a fallback in the same email. Password sign-in for customers (`signInCustomer`/`CustomerSignInForm`) is deleted outright, not just unrendered — confirmed absent from this build's compiled output by grep, after a red-team finding on an earlier round showed leaving it merely unrendered would have kept it live and callable, bypassing the new rate limiter entirely. New in-process rate limiting (`src/lib/auth/otp-rate-limit.ts`): 60s minimum gap per email before a resend, 5 requests/15min/email, 20/15min/IP, 10 verify attempts/15min/email, 40/15min/IP. Full review history (two rounds each of security and red-team, the stale-Server-Action HIGH finding and its fix, the auto-submit stale-DOM-read bug found and fixed during the follow-on `otp-animation` card's review) in `docs/HANDOVER.md` §45 (this branch) and §45/§46 on `release/rc-41-email-otp`/`release/rc-42-otp-animation`. **`otp-animation` (rc-42) remains unshipped**, waiting on the owner's review of its own preview links — it depends on this release's code boxes and cannot go out ahead of it. |
| 30 | 2026-09-25 12:12:43 UTC (service start; quiet-shop check passed ~12:09 UTC: 0 orders placed in the last 10 minutes, 0 in PENDING_PAYMENT) | `2775eb5` (merge commit `Merge release/rc-43-auth-v2` onto `kit-radix-nova`'s tip) | `kit-radix-nova` (from `release/rc-43-auth-v2` @ `881acbf`) | `44062766f26c2d0a` | **58 of 58** — new migration `0057_customers_org_user_unique.sql` (`ALTER TABLE customers ADD CONSTRAINT customers_org_user_unique UNIQUE(org_id, user_id)`), additive-only, drilled rollback (`supabase/rollback/0057_customers_org_user_unique.down.sql`). Pre-check passed: 0 existing `(org_id, user_id)` duplicates on production before applying | Claude Code session (integrator), owner go/no-go YES 2026-09-25, on top of a same-day live incident found, diagnosed and fixed mid-review — see HANDOVER §48 for the full account | Pass, signed out, GET only: `/` `/menu` `/sign-in` `/cart` `/robots.txt` `/sitemap.xml` `/api/health` `/manifest.webmanifest` `/apple-icon.png` `/account/sign-in` `/forgot-password` 200, `/order/not-a-uuid` 404, `/account/join` 307 (redirects to `/account/sign-in`), `/auth/confirm?code=stale` 307 (the friendly redirect — confirmed it never attempts to process the code), `/app/orders` `/app/deliveries` `/app/kds` `/app/staff` `/app/iq/live` `/app/iq` `/app/iq/pnl` `/app/iq/brief` 307. Local `.next/BUILD_ID` and commit both match the deployed values exactly. Service active, 0 restarts, 0 journal errors, `systemctl --failed` empty, no new nginx errors since the deploy | Not run by me: the owner is testing the fixed OTP flow live | Backup immediately before: `/var/backups/frybird/frybird-20260925-1209.dump`, 81 tables. Post-deploy fingerprint re-checked read-only: `4a3d000a67a52b7e857e320f2d79df2b`, 2 active OWNER — matches baseline, no anomaly. Staff invite flow confirmed, before deploying, not to go through `/auth/confirm` (`inviteUserByEmail` → `/accept-invite`, a separate URL-fragment mechanism, zero diff in this release). **Content of this release (`auth-v2` card, full detail in HANDOVER §47/§48):** customer sign-in/sign-up unified to one email-code screen (`shouldCreateUser:true`); staff/owner code-only password reset (`/forgot-password`) that revokes every other session on success; a "stay signed in" remember-me checkbox for both surfaces, working around a real `@supabase/ssr` cookie-lifetime bug; `/auth/confirm` kept as a permanent friendly redirect rather than deleted, so old emailed links never 404; three new Supabase email templates (`docs/email-templates/login-code.html`, `welcome.html`, `reset-code.html`); **plus the same-day fix for a live customer-sign-in incident** (every OTP auto-submit sent a stale, one-digit-short code, deterministically, since Release 29 — see HANDOVER §48). Reviews: security APPROVE (one fix, migration 0057), red-team BLOCK → two fixes → delta APPROVE, then a further delta security APPROVE on the live-incident fix. Known, accepted, non-blocking: the resend-notice banner staying on the old "wrong code" message instead of switching to "new code sent" if a verify error preceded the resend — parked as a follow-up per the owner. |
